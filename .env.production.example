# =============================================================================
# Bookie Production Environment Configuration
# =============================================================================
# Copy this file to .env.production and fill in the values.
# 
# ⚠️  IMPORTANT: This is your PRODUCTION environment.
#     Double-check all values before deploying!
# 
# SETUP INSTRUCTIONS:
# 
# 1. CONVEX PRODUCTION PROJECT:
#    - Go to https://dashboard.convex.dev
#    - Create a NEW project for production (e.g., "bookie-prod")
#    - Go to Settings > Deploy Keys
#    - Create a deploy key and copy it
#
# 2. FIREBASE PROJECT:
#    - Go to https://console.firebase.google.com
#    - Create or select your production project
#    - Enable Hosting
#    - (Optional) Set up custom domain
#
# 3. AUTH KEYS:
#    - Generate DIFFERENT keys for production!
#    - Run: bun run generateKeys.mjs
#    - Copy the generated values
# =============================================================================

# Convex Configuration (REQUIRED)
# Get from: Convex Dashboard > Project > Settings > Deploy Keys
CONVEX_DEPLOY_KEY=prod:bookie-prod|XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Convex URL (REQUIRED)
# Format: https://[deployment-name].convex.cloud
CONVEX_URL=https://your-production-deployment.convex.cloud

# Auth Configuration (REQUIRED)
# Generate using: bun run generateKeys.mjs
# ⚠️  Use DIFFERENT keys from preview!
CONVEX_AUTH_SECRET=your-production-auth-secret
JWT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRODUCTION_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----"
JWKS='{"keys":[{"use":"sig","e":"AQAB","kty":"RSA","n":"your-production-key-here"}]}'

# Client Configuration (REQUIRED)
# Should match CONVEX_URL
VITE_CONVEX_URL=https://your-production-deployment.convex.cloud

# Firebase Configuration (REQUIRED)
# Get from: Firebase Console > Project Settings > General
FIREBASE_PROJECT_ID=your-firebase-project-id

# Site URL for auth callbacks
# Use your custom domain if configured
CONVEX_SITE_URL=https://your-production-site.web.app
